<?xml version="1.0" encoding="UTF-8"?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="audioteca.LoginPage"
    Padding="20">
    <ScrollView Padding="0, 20, 0, 0">
        <StackLayout>
            <Label Text="Introduzca sus credenciales de acceso" FontSize="Medium" HorizontalTextAlignment="Center"></Label>
            <Entry Placeholder="Usuario" FontSize="Medium" Text="{Binding Username}"/>
            <Entry Placeholder="Contraseña" FontSize="Medium" IsPassword="True" Text="{Binding Password}"/>
            <Button
                Text="Acceder"
                FontSize="Medium"
                BackgroundColor="DodgerBlue"
                TextColor="White"
                Clicked="ButtonClick_Login" FontAttributes="Bold"></Button>
            <Label Text="{Binding ErrorMessage}" FontSize="Medium" HorizontalTextAlignment="Center" TextColor="Red"></Label>
        </StackLayout>
    </ScrollView>
</ContentPage>
